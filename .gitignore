# ── Dev tooling / dependencies (never part of the deployed game) ──
node_modules/
scratchpad/
.vscode/

# ── OS / editor junk ──
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# ── Backups ──
*.bak
*.orig
index.html.*

# ── Design / working docs: keep in VCS, but exclude from any production deploy bundle ──
# docs/ (design notes) and demo/ (standalone effect demos) are reference material, not the
# game. Ship only index.html + assets/ + audio/ to the game's public host.
